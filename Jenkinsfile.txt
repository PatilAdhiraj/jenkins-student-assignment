pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Compiling application...'
            }
        }
        stage('Test') {
            steps {
                echo 'Running unit tests... Pass!'
            }
        }
        stage('Package') {
            steps {
                // Windows-friendly file creation
                writeFile file: 'build-info.txt', text: "Build executed successfully on Windows."
            }
        }
    }
    
    post {
        success {
            echo 'Build successful! Ready for release.'
        }
    }
}